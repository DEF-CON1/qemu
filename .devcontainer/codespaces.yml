services:
  qemu:
    container_name: qemu
    image: ghcr.io/qemus/qemu
    environment:
      RAM_SIZE: "8"
      DISK_SIZE: "64"
      CPU_CORES: "2"
      AUDIO: "Y"
      BOOT: "https://downloads.raspberrypi.com/rpd_x86/images/rpd_x86-2022-07-04/2022-07-01-raspios-bullseye-i386.iso"
    devices:
      - /dev/kvm
      - /dev/net/tun
    cap_add:
      - NET_ADMIN
    ports:
      - 8006:8006
    volumes:
      - ./qemu:/storage
    restart: on-failure
    stop_grace_period: 2m
