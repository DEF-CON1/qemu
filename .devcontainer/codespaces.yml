services:
  qemu:
    container_name: qemu
    image: ghcr.io/qemus/qemu
    environment:
      RAM_SIZE: "16"
      DISK_SIZE: "100"
      CPU_CORES: "4"
      AUDIO: "Y"
    devices:
      - /dev/kvm
      - /dev/net/tun
    cap_add:
      - NET_ADMIN
    ports:
      - 8006:8006
    volumes:
      - ./qemu:/storage
    restart: on-failure
    stop_grace_period: 2m
